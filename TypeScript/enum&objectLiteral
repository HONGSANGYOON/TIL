enum고ㅏ object literal 비교하기

enumdms 
=> 열거형 데이터 타입
=> 상수의 그룹화를 위해서는 좋다
=> 코드가독성을 높이고 명확한 상수값을 정의할 수 있다.
=> *컴파일시 자동으로 숫자값을 매칭되므로 따로 값을 할당할 필요가 없다*

object literal
=> enum과 흡사하나, 키워드가 아니고 const(let도 사용 가능)라는 키워드를 사용한다.
=> 객체 리터럴이라 어떤 타입의 값도 대입을 할 수 있다.
=> 런타임 에러를 방지할 수 있다.

enum은 간ㄷ단한 상수값을 그룹화해서 관리할때,
object literal은 객체형식 즉, 그룹화의 코드에서 사용한다,.


