//화살표함수
//ES6 신 문법

function add(x, y) {
  return x + y;
}

// 기본적인 화살표 함수
let arrowfunc = (x, y) => {
  return x + y;
};

//한줄로
let arrowfunc2 = (x, y) => x + y;

function testfunc(x) {
  return x;
}

//위에껄 화살표 함수로
let arrowfunc3 = (x) => x;
